<!DOCTYPE html>

    <%- include('../partials/head'); %> <!-- pra trazer o conteudo que tá no head.ejs -->

    <body class="container">
        <%- include('../partials/header'); %>
        <main>
            <div class="jumbotron">
                <h1>Node com EJS</h1>
                <p>Trabalhando com arquivos EJS</p>
                <p> <% subtitle %> </p>
            </div>
            <ul>
                <% qualities.forEach(function(stop) { %>
                <li>
                    <strong><%= stop.title %></strong><%= stop.message %>
                </li>
                <% }) %>
            </ul>
        </main>
        <%- include('../partials/footer'); %>
    </body>
</html>